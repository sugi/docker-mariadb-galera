[mysqld]
default_storage_engine = InnoDB
#innodb_file_format = Barracuda # default is Barracuda from 10.2
#innodb_default_row_format = Dynamic # Default is Dynamic from 10.2.2
innodb_file_per_table
innodb_large_prefix
